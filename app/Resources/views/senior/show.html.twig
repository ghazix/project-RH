{% extends 'base.html.twig' %}

{% block body %}
    <h1>Senior</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ senior.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ senior.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ senior.prenom }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ senior.username }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ senior.email }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ senior.password }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('Ssenior_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('Ssenior_edit', { 'id': senior.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
